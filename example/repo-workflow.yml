name: Wiz Security Scan

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  wiz-scan:
    uses: my-org/security-platform/.github/workflows/wiz-scan.yml@main
    with:
      scan_path: .
    secrets: inherit
